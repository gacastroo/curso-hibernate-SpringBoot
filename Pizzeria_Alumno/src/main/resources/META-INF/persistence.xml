<persistence xmlns="https://jakarta.ee/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="https://jakarta.ee/xml/ns/persistence
                                 https://jakarta.ee/xml/ns/persistence/persistence_3_0.xsd"
             version="3.0">

    <persistence-unit name="pizzeria-pu" transaction-type="RESOURCE_LOCAL">        <!-- Clases anotadas JPA -->
        <class>com.pizzeria.modelo.Pizza</class>
        <class>com.pizzeria.modelo.Cliente</class>
        <class>com.pizzeria.modelo.Pedido</class>

        <!-- Configuración del proveedor JPA (Hibernate) -->
        <properties>
            <!-- Driver y URL de H2 en memoria -->
            <property name="jakarta.persistence.jdbc.driver" value="org.h2.Driver"/>
            <property name="jakarta.persistence.jdbc.url" value="jdbc:h2:mem:pizzeriadb;DB_CLOSE_DELAY=-1"/>
            <property name="jakarta.persistence.jdbc.user" value="sa"/>
            <property name="jakarta.persistence.jdbc.password" value=""/>

            <!-- Dialecto de Hibernate para H2 -->
            <property name="hibernate.dialect" value="org.hibernate.dialect.H2Dialect"/>

            <!-- Crear tablas automáticamente -->
            <property name="hibernate.hbm2ddl.auto" value="create"/>

            <!-- Mostrar SQL en consola -->
            <property name="hibernate.show_sql" value="true"/>
            <property name="hibernate.format_sql" value="true"/>
        </properties>
    </persistence-unit>
</persistence>